class Dataset:
    r"""
    base dataset class.

    Returns:
        graph(s), idx_train, idx_val, idx_test
    """

    def __init__(self) -> None:
        pass

    def preprocess():
        # data = load(name)
        # graph, idx = process(data)
        raise NotImplementedError
