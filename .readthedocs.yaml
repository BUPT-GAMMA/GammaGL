version: 2

submodules:
  include: all
  recursive: true

build:
  os: ubuntu-20.04
  tools:
    python: "3.9"

python:
  install:
    - requirements: docs/requirements.txt
    - method: setuptools
      path: .

